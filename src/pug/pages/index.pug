extends ../layout/main

block variables
  - var title = 'Webpack + Pug';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';

block content
  section.section__slider
    .container.container__slider
      .slider
        +inside('наши проекты')
        +switch
      .slider
        .slide
          .slide__img
          +slide__desc('ССК “ОЛИМП”', 'Световое оформление форума “Шубы-2019”')
        .slide
          .slide__img.slide__img_right
          +slide__desc('ССК “ОЛИМП”', 'Световое оформление форума “Шубы-2019”')

  section
    .container
      +inside('каталог продукции')
    .container
      +inside('аренда оборудования')
    .container
      +inside('новости')
      //- +item('item title', 'item descr')
      //- +item('item title', 'item descr')
      //- +item('item title', 'item descr')
  //- example-component

block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }